&НаКлиенте
Процедура ВС_ПриОткрытииПосле(Отказ)

    // When opening a customer order, the presence of a linked barcode card is checked
    // A label is displayed on the form as "Barcode: " + barcode representation
    // If no barcode card is linked, only the text "Barcode:" is shown

    Штрихкарта = ПолучитьНаименованиеПривязаннойШтрихкарты();
    Элемент.ПривязаннаяШтрихкарта.Заголовок = "Штрихкарта: " + Штрихкарта;

КонецПроцедуры
